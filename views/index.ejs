<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Пациенты</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-4">

  <h2 class="mb-4">Список пациентов</h2>

  <ul class="list-group mb-4">
    <% patients.forEach(p => { %>
      <li class="list-group-item">
        <strong><%= p.fullName %></strong> — <%= p.birthDate %> — <%= p.gender %>
      </li>
    <% }) %>
  </ul>

  <a class="btn btn-primary me-2" href="/appointments">Перейти к приёмам</a>
  <a class="btn btn-secondary" href="/history">История болезней</a>

</body>
</html>
